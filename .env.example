# Shoal backend environment variables
# Copy this file to .env and fill in your values.
# The server does NOT require these — API keys are supplied by the user at
# runtime via the Settings → Connect Alpaca flow and held only in memory.

# Port the backend listens on (default: 3001)
PORT=3001
